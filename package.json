{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "description": "LittleDevil personal site — Markdown blog → static HTML → Cloudflare Pages",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && npx --yes serve dist -p 4173",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "marked": "^15.0.0"
  }
}
